/*
Needle Haystack:
Implement a program which finds all the indices of occurrence
of a given substring(needle) inside a larger string(haystack).

Input:
The first line will contain the haystack; [1; 5000000].
The second line will contain the needle; [2; 100].

Output:
On separate lines, output the indices of occurrence of the needle.
*/

#include <iostream>
#include <string>
using namespace std;

int main() {

    cin.get();
    return 0;
}
